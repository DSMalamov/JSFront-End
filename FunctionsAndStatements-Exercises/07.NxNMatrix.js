function matrix(n) {
  for (let index = 0; index < n; index++) {
    const arr = [];

    for (let index = 0; index < n; index++) {
      arr.push(n);
    }

    console.log(arr.join(" "));
  }
}

matrix(7);
